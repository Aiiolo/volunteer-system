# 三江学院志愿者服务系统设计文档

## 1. 文档说明

本文档用于说明“三江学院志愿者服务系统”的系统背景、需求分析、总体架构、功能设计、数据库设计、接口设计、安全设计、部署设计和后续扩展方向。

本文档适用于：

课程设计

---

## 2. 项目背景

高校志愿服务活动通常涉及活动发布、志愿者报名、报名审核、现场签到、服务时长统计、活动总结等多个环节。传统线下管理方式存在信息分散、审核效率低、服务记录不完整、统计困难等问题。

本系统以“三江学院志愿者服务”为业务场景，设计并实现一个多角色志愿服务管理平台。系统通过前端页面展示和后端 REST API 服务，实现志愿者、管理员、活动负责人三类角色的基础业务闭环。

---

## 3. 建设目标

系统建设目标如下：

1. 实现志愿者注册、登录、资料维护。
2. 实现管理员对志愿者账号的审核和管理。
3. 实现管理员对志愿活动的创建、发布、修改和删除。
4. 实现志愿者浏览活动、提交报名、查看报名状态。
5. 实现管理员审核志愿者报名申请。
6. 实现志愿者签到、签退和服务时长自动统计。
7. 实现活动负责人现场辅助签到签退、评价志愿者、提交活动总结。
8. 实现基于 JWT 的登录认证和基于角色的权限控制。
9. 实现统一接口响应和统一异常处理，提高系统可维护性。

---

## 4. 用户角色设计

系统包含三类核心角色。

### 4.1 志愿者

志愿者是普通用户，主要使用前台功能。

主要权限：

- 注册账号
- 登录系统
- 浏览已发布活动
- 报名活动
- 查看报名状态
- 活动签到
- 活动签退
- 查看个人资料和服务记录
- 修改个人资料

### 4.2 管理员

管理员是平台后台管理人员，负责系统基础数据维护和审核工作。

主要权限：

- 查看志愿者列表
- 审核志愿者账号
- 修改志愿者资料
- 创建活动
- 修改活动
- 删除活动
- 审核活动报名
- 查看统计数据

### 4.3 活动负责人

活动负责人负责具体活动的现场管理和活动总结。

主要权限：

- 查看本人负责的活动
- 协助志愿者签到
- 协助志愿者签退
- 评价志愿者表现
- 提交活动总结

---

## 5. 需求分析

### 5.1 功能性需求

#### 5.1.1 用户认证需求

- 系统应支持用户登录。
- 登录成功后应返回 JWT Token。
- 用户访问受保护接口时应携带 Token。
- 系统应根据用户角色控制接口访问权限。

#### 5.1.2 志愿者管理需求

- 志愿者可提交注册信息。
- 新注册志愿者默认为待审核状态。
- 管理员可审核志愿者账号。
- 管理员可禁用志愿者账号。
- 志愿者可维护个人资料。

#### 5.1.3 活动管理需求

- 管理员可创建活动。
- 管理员可修改活动信息。
- 管理员可发布活动。
- 管理员可删除活动。
- 志愿者只能查看已发布活动。

#### 5.1.4 活动报名需求

- 志愿者可报名已发布活动。
- 系统应防止重复报名。
- 系统应限制活动报名人数。
- 报名申请需管理员审核。
- 志愿者可查看报名状态。

#### 5.1.5 签到签退需求

- 报名通过后志愿者可签到。
- 志愿者签到后可签退。
- 系统应自动计算服务时长。
- 活动负责人可协助志愿者签到签退。

#### 5.1.6 活动总结需求

- 活动负责人可提交活动总结。
- 每个活动只能提交一次总结。
- 提交总结后活动状态更新为已完成。

#### 5.1.7 数据统计需求

- 管理员可查看志愿者总数。
- 管理员可查看活动总数。
- 管理员可查看累计服务时长。
- 管理员可查看报名总数和通过率。

### 5.2 非功能性需求


| 类型    | 要求                                    |
| ----- | ------------------------------------- |
| 安全性   | 使用 JWT 认证，使用 BCrypt 存储密码              |
| 可维护性  | 采用 Controller、Service、Repository 分层结构 |
| 可扩展性  | 后端使用 RESTful API，便于前端或移动端接入           |
| 一致性   | 统一响应格式，统一异常处理                         |
| 数据完整性 | 使用数据库主键、外键、唯一约束和检查约束                  |
| 可部署性  | 使用 Maven 管理依赖，Spring Boot 独立启动        |


---

## 6. 总体架构设计

### 6.1 技术架构

系统采用前后端分离思路。

```text
浏览器前端
  ↓ HTTP/JSON
Spring Boot 后端服务
  ↓ JPA
MySQL 数据库
  ↓
Redis 缓存服务
```

### 6.2 前端架构

前端采用静态页面方式实现：

- HTML：页面结构
- CSS：页面样式
- JavaScript：页面跳转、简单交互
- localStorage：临时保存用户选择的角色
- sessionStorage：保存会话相关信息

前端页面分为：

```text
首页与登录页面
志愿者端页面
管理员端页面
活动负责人端页面
```

### 6.3 后端架构

后端采用 Spring Boot 分层架构：

```text
Controller 层：接收请求，返回响应
Service 层：处理业务逻辑和事务
Repository 层：数据库访问
Entity 层：数据库实体映射
DTO 层：接口入参与出参对象
Security 层：JWT 认证和权限控制
Exception 层：统一异常处理
Common 层：统一响应结构
```

### 6.4 后端包结构

```text
com.sanjuan.volunteer
├── VolunteerSystemApplication.java
├── common
│   └── ApiResponse.java
├── config
│   └── SecurityConfig.java
├── controller
│   ├── VolunteerApiController.java
│   ├── AdminApiController.java
│   └── OrganizerApiController.java
├── dto
│   └── Dtos.java
├── entity
│   ├── SysUser.java
│   ├── VolunteerProfile.java
│   ├── Activity.java
│   ├── ActivityApplication.java
│   ├── AttendanceRecord.java
│   ├── ActivitySummary.java
│   └── Enums.java
├── exception
│   ├── BusinessException.java
│   └── GlobalExceptionHandler.java
├── repository
├── security
└── service
```

---

## 7. 功能模块设计

### 7.1 用户认证模块

#### 7.1.1 模块职责

- 用户登录
- JWT 生成
- JWT 校验
- 当前用户身份解析
- 登录状态判断

#### 7.1.2 登录流程

```text
用户提交用户名和密码
  ↓
后端查询用户信息
  ↓
使用 BCrypt 校验密码
  ↓
检查账号状态是否 ACTIVE
  ↓
生成 JWT Token
  ↓
返回 Token 和用户基本信息
```

#### 7.1.3 权限控制

系统通过 Spring Security 实现 RBAC 权限控制。


| 接口路径                              | 访问角色      |
| --------------------------------- | --------- |
| `/api/v1/volunteer/auth/register` | 匿名访问      |
| `/api/v1/volunteer/auth/login`    | 匿名访问      |
| `/api/v1/volunteer/**`            | 志愿者、管理员   |
| `/api/v1/admin/**`                | 管理员       |
| `/api/v1/organizer/**`            | 活动负责人、管理员 |


---

### 7.2 志愿者模块

#### 7.2.1 模块职责

- 志愿者注册
- 个人资料维护
- 服务记录查看
- 服务时长查看

#### 7.2.2 注册流程

```text
志愿者填写注册信息
  ↓
系统检查用户名是否重复
  ↓
密码 BCrypt 加密
  ↓
创建 sys_user 记录，状态为 PENDING
  ↓
创建 volunteer_profile 档案
  ↓
等待管理员审核
```

#### 7.2.3 个人资料设计

志愿者资料包含：

- 真实姓名
- 年龄
- 技能特长
- 服务偏好
- 累计服务时长

---

### 7.3 活动管理模块

#### 7.3.1 模块职责

- 活动创建
- 活动修改
- 活动发布
- 活动删除
- 活动列表查询

#### 7.3.2 活动状态流转

```text
DRAFT 草稿
  ↓
PUBLISHED 已发布
  ↓
ONGOING 进行中
  ↓
COMPLETED 已完成
```

#### 7.3.3 业务规则

1. 新建活动默认为 `DRAFT`。
2. 只有 `PUBLISHED` 状态的活动可被志愿者查询和报名。
3. 活动结束并提交总结后，状态变为 `COMPLETED`。
4. 管理员可以修改活动基础信息和状态。

---

### 7.4 活动报名模块

#### 7.4.1 模块职责

- 志愿者报名活动
- 查询报名状态
- 管理员审核报名
- 控制活动人数

#### 7.4.2 报名流程

```text
志愿者选择活动
  ↓
系统检查活动是否已发布
  ↓
系统检查是否重复报名
  ↓
系统检查活动名额是否已满
  ↓
创建报名记录，状态为 PENDING
  ↓
管理员审核
  ↓
状态更新为 APPROVED 或 REJECTED
```

#### 7.4.3 报名状态


| 状态         | 说明   |
| ---------- | ---- |
| `PENDING`  | 待审核  |
| `APPROVED` | 审核通过 |
| `REJECTED` | 审核拒绝 |


---

### 7.5 签到签退模块

#### 7.5.1 模块职责

- 志愿者签到
- 志愿者签退
- 活动负责人辅助签到签退
- 自动计算服务时长
- 累加志愿者总服务时长

#### 7.5.2 签到签退流程

```text
志愿者或活动负责人发起签到
  ↓
系统检查报名是否已通过
  ↓
记录签到时间
  ↓
活动结束后发起签退
  ↓
记录签退时间
  ↓
计算服务时长
  ↓
累加到志愿者总服务时长
```

#### 7.5.3 服务时长计算

服务时长通过签退时间减去签到时间计算，单位为小时，保留两位小数。

---

### 7.6 活动总结模块

#### 7.6.1 模块职责

- 活动负责人提交活动总结
- 保存活动反馈
- 活动完成状态更新

#### 7.6.2 总结规则

1. 活动负责人只能总结自己负责的活动。
2. 每个活动只能提交一次总结。
3. 提交总结后活动状态更新为 `COMPLETED`。

---

### 7.7 数据统计模块

#### 7.7.1 模块职责

- 统计志愿者数量
- 统计活动数量
- 统计累计服务时长
- 统计报名数量
- 统计报名通过率

#### 7.7.2 统计指标


| 指标                | 说明     |
| ----------------- | ------ |
| volunteerCount    | 志愿者总数  |
| activityCount     | 活动总数   |
| totalServiceHours | 累计服务时长 |
| applicationCount  | 报名总数   |
| approvedCount     | 审核通过数  |
| approvalRate      | 报名通过率  |


---

## 8. 数据库设计

### 8.1 数据库名称

```text
volunteer_system
```

### 8.2 数据表总览


| 表名                     | 说明      |
| ---------------------- | ------- |
| `sys_user`             | 系统用户表   |
| `volunteer_profile`    | 志愿者档案表  |
| `activity`             | 志愿活动表   |
| `activity_application` | 活动报名表   |
| `attendance_record`    | 签到签退记录表 |
| `activity_summary`     | 活动总结表   |


### 8.3 用户表 `sys_user`


| 字段          | 类型           | 说明          |
| ----------- | ------------ | ----------- |
| id          | BIGINT       | 主键          |
| username    | VARCHAR(64)  | 用户名，唯一      |
| password    | VARCHAR(100) | BCrypt 加密密码 |
| role        | ENUM         | 用户角色        |
| status      | ENUM         | 用户状态        |
| create_time | DATETIME     | 创建时间        |
| update_time | DATETIME     | 更新时间        |


角色取值：


| 值           | 说明    |
| ----------- | ----- |
| `VOLUNTEER` | 志愿者   |
| `ADMIN`     | 管理员   |
| `ORGANIZER` | 活动负责人 |


用户状态：


| 值          | 说明  |
| ---------- | --- |
| `PENDING`  | 待审核 |
| `ACTIVE`   | 启用  |
| `DISABLED` | 禁用  |


### 8.4 志愿者档案表 `volunteer_profile`


| 字段          | 类型           | 说明       |
| ----------- | ------------ | -------- |
| user_id     | BIGINT       | 主键，关联用户表 |
| real_name   | VARCHAR(64)  | 真实姓名     |
| age         | INT          | 年龄       |
| skills      | VARCHAR(512) | 技能特长     |
| preferences | VARCHAR(512) | 服务偏好     |
| total_hours | DECIMAL(8,2) | 累计服务时长   |
| create_time | DATETIME     | 创建时间     |
| update_time | DATETIME     | 更新时间     |


### 8.5 活动表 `activity`


| 字段             | 类型            | 说明       |
| -------------- | ------------- | -------- |
| id             | BIGINT        | 主键       |
| title          | VARCHAR(128)  | 活动标题     |
| description    | TEXT          | 活动描述     |
| requirements   | VARCHAR(1024) | 活动要求     |
| status         | ENUM          | 活动状态     |
| organizer_id   | BIGINT        | 活动负责人 ID |
| max_volunteers | INT           | 最大招募人数   |
| start_time     | DATETIME      | 开始时间     |
| end_time       | DATETIME      | 结束时间     |
| create_time    | DATETIME      | 创建时间     |
| update_time    | DATETIME      | 更新时间     |


活动状态：


| 值           | 说明  |
| ----------- | --- |
| `DRAFT`     | 草稿  |
| `PUBLISHED` | 已发布 |
| `ONGOING`   | 进行中 |
| `COMPLETED` | 已完成 |


### 8.6 活动报名表 `activity_application`


| 字段           | 类型       | 说明       |
| ------------ | -------- | -------- |
| id           | BIGINT   | 主键       |
| activity_id  | BIGINT   | 活动 ID    |
| volunteer_id | BIGINT   | 志愿者用户 ID |
| status       | ENUM     | 报名状态     |
| apply_time   | DATETIME | 报名时间     |
| audit_time   | DATETIME | 审核时间     |


约束：

- `activity_id + volunteer_id` 唯一，防止重复报名。

### 8.7 签到签退记录表 `attendance_record`


| 字段             | 类型            | 说明       |
| -------------- | ------------- | -------- |
| id             | BIGINT        | 主键       |
| activity_id    | BIGINT        | 活动 ID    |
| volunteer_id   | BIGINT        | 志愿者用户 ID |
| check_in_time  | DATETIME      | 签到时间     |
| check_out_time | DATETIME      | 签退时间     |
| hours          | DECIMAL(6,2)  | 本次服务时长   |
| evaluation     | VARCHAR(1024) | 活动评价     |
| create_time    | DATETIME      | 创建时间     |
| update_time    | DATETIME      | 更新时间     |


约束：

- `activity_id + volunteer_id` 唯一，防止同一活动重复创建签到记录。

### 8.8 活动总结表 `activity_summary`


| 字段           | 类型       | 说明       |
| ------------ | -------- | -------- |
| id           | BIGINT   | 主键       |
| activity_id  | BIGINT   | 活动 ID，唯一 |
| organizer_id | BIGINT   | 活动负责人 ID |
| content      | TEXT     | 总结内容     |
| feedback     | TEXT     | 反馈建议     |
| submit_time  | DATETIME | 提交时间     |


---

## 9. 接口设计

### 9.1 统一响应格式

系统所有接口统一返回：

```json
{
  "success": true,
  "message": "success",
  "data": {},
  "timestamp": "2026-05-19T10:00:00"
}
```

字段说明：


| 字段        | 说明     |
| --------- | ------ |
| success   | 请求是否成功 |
| message   | 提示信息   |
| data      | 响应数据   |
| timestamp | 响应时间   |


### 9.2 志愿者端接口


| 方法   | 路径                                       | 功能      | 权限              |
| ---- | ---------------------------------------- | ------- | --------------- |
| POST | `/api/v1/volunteer/auth/register`        | 志愿者注册   | 匿名              |
| POST | `/api/v1/volunteer/auth/login`           | 登录      | 匿名              |
| GET  | `/api/v1/volunteer/activities`           | 查看可报名活动 | VOLUNTEER/ADMIN |
| POST | `/api/v1/volunteer/applications`         | 活动报名    | VOLUNTEER/ADMIN |
| GET  | `/api/v1/volunteer/applications/status`  | 查看报名状态  | VOLUNTEER/ADMIN |
| GET  | `/api/v1/volunteer/activities/my`        | 我的活动    | VOLUNTEER/ADMIN |
| POST | `/api/v1/volunteer/attendance/check-in`  | 签到      | VOLUNTEER/ADMIN |
| POST | `/api/v1/volunteer/attendance/check-out` | 签退      | VOLUNTEER/ADMIN |
| GET  | `/api/v1/volunteer/profile`              | 查看个人中心  | VOLUNTEER/ADMIN |
| PUT  | `/api/v1/volunteer/profile`              | 修改个人资料  | VOLUNTEER/ADMIN |


### 9.3 管理员端接口


| 方法     | 路径                                            | 功能      | 权限    |
| ------ | --------------------------------------------- | ------- | ----- |
| GET    | `/api/v1/admin/volunteers`                    | 志愿者列表   | ADMIN |
| POST   | `/api/v1/admin/volunteers/audit`              | 审核志愿者   | ADMIN |
| PUT    | `/api/v1/admin/volunteers/{id}`               | 修改志愿者资料 | ADMIN |
| POST   | `/api/v1/admin/activities`                    | 创建活动    | ADMIN |
| PUT    | `/api/v1/admin/activities/{id}`               | 修改活动    | ADMIN |
| DELETE | `/api/v1/admin/activities/{id}`               | 删除活动    | ADMIN |
| GET    | `/api/v1/admin/activities/{id}/applications`  | 查看报名记录  | ADMIN |
| POST   | `/api/v1/admin/activities/applications/audit` | 审核报名    | ADMIN |
| GET    | `/api/v1/admin/dashboard/stats`               | 数据统计    | ADMIN |


### 9.4 活动负责人端接口


| 方法   | 路径                                           | 功能       | 权限              |
| ---- | -------------------------------------------- | -------- | --------------- |
| GET  | `/api/v1/organizer/my-activities`            | 查看本人负责活动 | ORGANIZER/ADMIN |
| POST | `/api/v1/organizer/attendance/assist-check`  | 辅助签到签退   | ORGANIZER/ADMIN |
| PUT  | `/api/v1/organizer/attendance/{id}/evaluate` | 评价志愿者    | ORGANIZER/ADMIN |
| POST | `/api/v1/organizer/activities/{id}/summary`  | 提交活动总结   | ORGANIZER/ADMIN |


---

## 10. 安全设计

### 10.1 密码安全

用户密码不以明文保存，系统使用 BCrypt 进行哈希处理。

密码校验流程：

```text
用户输入密码
  ↓
后端读取数据库中的 BCrypt 密文
  ↓
调用 PasswordEncoder.matches 进行比较
  ↓
返回校验结果
```

### 10.2 JWT 认证

JWT 中包含：

- 用户 ID
- 用户名
- 用户角色
- 过期时间

请求受保护接口时，前端需携带：

```text
Authorization: Bearer <token>
```

### 10.3 RBAC 权限控制

系统采用基于角色的访问控制。


| 角色        | 权限范围              |
| --------- | ----------------- |
| VOLUNTEER | 志愿者端接口            |
| ADMIN     | 管理员端接口，也可访问部分其他接口 |
| ORGANIZER | 活动负责人端接口          |


### 10.4 异常处理安全

系统通过全局异常处理器捕获异常，对外只返回业务提示，不向前端暴露堆栈信息和数据库错误细节。

---

## 11. 事务设计

涉及多表更新或关键业务状态变更的操作使用事务控制。


| 操作    | 涉及表                                     | 事务原因         |
| ----- | --------------------------------------- | ------------ |
| 志愿者注册 | `sys_user`、`volunteer_profile`          | 确保账号和档案同时创建  |
| 报名活动  | `activity_application`                  | 防止重复报名和名额异常  |
| 审核报名  | `activity_application`                  | 确保审核状态一致     |
| 签退    | `attendance_record`、`volunteer_profile` | 同时记录时长并累加总时长 |
| 提交总结  | `activity_summary`、`activity`           | 保存总结并更新活动状态  |


---

## 12. 部署设计

### 12.1 开发环境部署

开发环境可使用本机启动：

```text
浏览器打开 frontend/index.html
后端通过 mvn spring-boot:run 启动
MySQL 和 Redis 本地运行
```

### 12.2 后端启动流程

```text
安装 JDK 17
  ↓
安装 Maven
  ↓
安装并启动 MySQL
  ↓
安装并启动 Redis
  ↓
执行 schema.sql 和 data.sql
  ↓
修改 application.yml
  ↓
执行 mvn spring-boot:run
```

### 12.3 生产部署建议

如果后续上线，建议：

1. 前端部署到 Nginx。
2. 后端打包成 Jar 后部署到服务器。
3. MySQL 单独部署并定期备份。
4. Redis 设置访问密码。
5. JWT 密钥通过环境变量配置。
6. 开启 HTTPS。
7. 使用日志系统保存访问日志和错误日志。

---

## 13. 测试设计

### 13.1 功能测试


| 测试项       | 预期结果           |
| --------- | -------------- |
| 志愿者注册     | 注册成功，状态为待审核    |
| 志愿者未审核登录  | 登录失败           |
| 管理员审核志愿者  | 状态变为启用         |
| 志愿者登录     | 返回 JWT Token   |
| 志愿者报名活动   | 生成待审核报名记录      |
| 重复报名      | 系统提示已报名        |
| 管理员审核报名   | 报名状态变为通过或拒绝    |
| 志愿者签到     | 生成签到时间         |
| 志愿者签退     | 生成签退时间并计算服务时长  |
| 活动负责人提交总结 | 生成总结记录，活动变为已完成 |


### 13.2 权限测试


| 测试项          | 预期结果 |
| ------------ | ---- |
| 未登录访问志愿者接口   | 拒绝访问 |
| 志愿者访问管理员接口   | 拒绝访问 |
| 管理员访问管理员接口   | 允许访问 |
| 活动负责人访问本人活动  | 允许访问 |
| 活动负责人管理非本人活动 | 拒绝操作 |


### 13.3 边界测试


| 测试项          | 预期结果 |
| ------------ | ---- |
| 活动名额已满继续审核通过 | 系统拒绝 |
| 未报名直接签到      | 系统拒绝 |
| 未签到直接签退      | 系统拒绝 |
| 重复签到         | 系统拒绝 |
| 重复签退         | 系统拒绝 |
| 重复提交活动总结     | 系统拒绝 |


---

## 14. 系统特点

1. 采用前后端分离思想，接口职责清晰。
2. 后端使用 Spring Boot 标准分层结构，便于维护。
3. 使用 JWT 实现无状态认证，适合 REST API。
4. 使用 RBAC 控制三类用户权限。
5. 使用 BCrypt 保护用户密码安全。
6. 使用 DTO 隔离接口模型和数据库实体。
7. 使用事务保证关键业务数据一致性。
8. 使用数据库外键和唯一约束保证基础数据完整性。

---

## 15. 后续扩展方向

后续可扩展以下功能：

1. 前端页面接入真实后端接口。
2. 增加消息通知表和通知接口。
3. 增加社区论坛发帖、评论、审核功能。
4. 增加活动图片上传功能。
5. 增加服务证书导出功能。
6. 增加服务时长排行榜。
7. 增加管理员操作日志。
8. 增加验证码登录或短信登录。
9. 增加 Redis Token 黑名单，实现主动退出登录。
10. 增加单元测试和接口自动化测试。

---

## 16. 框架对照完成情况

对照“志愿者招募与活动管理系统框架”，当前系统完成情况如下：


| 框架项      | 完成情况    | 说明                                   |
| -------- | ------- | ------------------------------------ |
| 前端层      | 已完成核心页面 | 覆盖志愿者、管理员、活动负责人三类用户 PC 页面            |
| 业务逻辑层    | 已完成核心业务 | 注册登录、报名审核、活动管理、签到签退、评价总结、统计导出        |
| 数据层      | 已完成核心表  | 用户、志愿者档案、活动、报名、签到、总结表已建模             |
| 基础支撑层    | 已完成基础支撑 | Spring Boot、MySQL、JWT、RBAC、统一异常、CORS |
| 志愿者注册登录  | 已完成     | 支持前端注册、登录、管理员审核后启用                   |
| 招募报名     | 已完成     | 支持浏览活动、报名、查询状态                       |
| 活动参与     | 已完成     | 支持我的活动、签到签退、通知查看                     |
| 个人中心     | 已完成     | 支持资料维护、服务时长和记录查询                     |
| 管理员志愿者管理 | 已完成     | 支持列表、搜索、审核、编辑、禁用                     |
| 管理员活动管理  | 已完成     | 支持创建、编辑、删除、发布、报名审核                   |
| 数据统计     | 已完成     | 支持核心指标统计和 CSV 导出                     |
| 活动负责人管理  | 已完成     | 支持本人活动、报名名单、现场签到签退、评价、总结             |
| 安全保障     | 已完成基础能力 | BCrypt 密码、JWT 鉴权、RBAC 权限、全局异常处理      |


---

## 17. 文档版本


| 版本   | 日期         | 说明                            |
| ---- | ---------- | ----------------------------- |
| V1.0 | 2026-05-19 | 完成系统总体设计、功能设计、数据库设计、接口设计和安全设计 |


