# 三江学院志愿者服务系统操作文档

## 1. 文档说明

本文档面向“三江学院志愿者服务系统”的最终使用者、部署人员和演示人员，说明系统访问方式、角色功能、常用操作流程、接口调用方式、部署启动步骤和常见问题处理方法。

系统包含三类用户角色：

1. 志愿者
2. 管理员
3. 活动负责人

不同角色登录后进入不同的功能页面，拥有不同的操作权限。

---

## 2. 系统运行环境

### 2.1 前端运行环境

前端为静态页面项目，不依赖 Node.js、Vue、React 等框架，可直接通过浏览器打开。

建议浏览器：

- Microsoft Edge
- Google Chrome
- Firefox

### 2.2 后端运行环境

后端为 Spring Boot 项目，建议环境如下：

| 环境 | 版本 |
|---|---|
| JDK | 17 |
| Maven | 3.8 及以上 |
| MySQL | 8.x |
| Redis | 7.x |
| Spring Boot | 3.x |

### 2.3 默认服务地址

后端默认启动端口：

```text
http://localhost:8080
```

前端页面入口：

```text
frontend/index.html
```

---

## 3. 项目目录说明

项目主要分为前端和后端两部分：

```text
volunteer-system
├── frontend              # 前端静态页面
├── backend               # Spring Boot 后端服务
├── 操作文档.md            # 用户操作文档
└── 设计文档.md            # 系统设计文档
```

### 3.1 前端目录

```text
frontend
├── index.html                 # 系统首页
├── role-select.html           # 角色选择页
├── login.html                 # 登录页
├── volunteer*.html            # 志愿者端页面
├── admin*.html                # 管理员端页面
└── active*.html               # 活动负责人端页面
```

### 3.2 后端目录

```text
backend
├── pom.xml
└── src/main
    ├── java/com/sanjuan/volunteer
    │   ├── VolunteerSystemApplication.java
    │   ├── common
    │   ├── config
    │   ├── controller
    │   ├── dto
    │   ├── entity
    │   ├── exception
    │   ├── repository
    │   ├── security
    │   └── service
    └── resources
        ├── application.yml
        └── db
            ├── schema.sql
            └── data.sql
```

---

## 4. 系统安装与启动

### 4.1 初始化数据库

进入 MySQL 后，依次执行：

```sql
source backend/src/main/resources/db/schema.sql;
source backend/src/main/resources/db/data.sql;
```

如果使用图形化数据库工具，也可以打开上述两个 SQL 文件，按顺序执行。

数据库默认名称：

```text
volunteer_system
```

### 4.2 修改后端配置

打开配置文件：

```text
backend/src/main/resources/application.yml
```

根据本机环境修改数据库账号密码：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/volunteer_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
```

如果 Redis 地址或端口不同，也需要同步修改：

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
```

### 4.3 启动后端

进入后端目录：

```bash
cd backend
```

执行启动命令：

```bash
mvn spring-boot:run
```

看到类似如下信息表示启动成功：

```text
Tomcat started on port 8080
Started VolunteerSystemApplication
```

### 4.4 编译检查

如需检查项目是否能正常编译，可执行：

```bash
mvn -q -DskipTests compile
```

### 4.5 打开前端

用浏览器打开：

```text
frontend/index.html
```

系统首页打开后，点击登录入口即可进入角色选择和登录流程。

---

## 5. 默认测试账号

初始化数据中提供以下演示账号，默认密码均为：

```text
123456
```

| 账号 | 角色 | 说明 |
|---|---|---|
| admin | 管理员 | 后台管理账号 |
| organizer | 活动负责人 | 活动现场和总结管理账号 |
| volunteer | 志愿者 | 普通志愿者账号 |

> 说明：后端密码使用 BCrypt 加密存储，数据库中不会保存明文密码。

---

## 6. 系统访问入口

### 6.1 前端页面入口

打开项目中的前端首页：

```text
frontend/index.html
```

首页展示系统名称、宣传标语和登录入口。

### 6.2 登录流程

用户使用系统的一般流程如下：

```text
首页 index.html
  ↓
选择登录身份 role-select.html
  ↓
登录 login.html
  ↓
进入对应角色首页
```

角色首页对应关系：

| 角色 | 登录后页面 |
|---|---|
| 志愿者 | `frontend/volunteer.html` |
| 管理员 | `frontend/admin.html` |
| 活动负责人 | `frontend/active.html` |

---

## 7. 通用操作

### 7.1 选择身份

进入 `role-select.html` 后，可以选择以下身份：

- 志愿者
- 管理员
- 活动负责人

点击对应身份卡片后，系统会保存当前选择的角色，并跳转到登录页面。

### 7.2 登录系统

进入 `login.html` 后，页面标题会根据上一步选择的角色自动变化，例如：

- 志愿者登录
- 管理员登录
- 活动负责人登录

用户输入账号、密码和验证码后，点击“登录”按钮进入对应系统首页。

> 说明：当前前端页面仍以页面跳转为主，后端已经提供 JWT 登录接口，后续可将登录按钮改为调用后端登录接口。

### 7.3 退出登录

各角色首页右上角均提供“退出登录”按钮。

点击后会清除当前会话信息，并返回身份选择页面。

### 7.4 JWT 使用说明

后端登录成功后会返回 JWT Token。访问受保护接口时，需要在请求头中携带：

```text
Authorization: Bearer <token>
```

如果没有携带 Token，或 Token 无效、过期，系统会拒绝访问。

---

## 8. 志愿者端操作说明

志愿者端主要面向普通志愿者用户，用于浏览活动、报名活动、查看报名状态、签到签退、维护个人资料等。

### 8.1 志愿者首页

入口页面：

```text
frontend/volunteer.html
```

主要内容：

- 平台顶部导航
- 志愿者身份信息
- 搜索栏
- 轮播图
- 最新志愿活动展示

导航菜单包括：

| 菜单 | 页面 | 说明 |
|---|---|---|
| 平台首页 | `volunteer.html` | 查看平台首页和推荐活动 |
| 关于我们 | `volunteer-about.html` | 查看平台介绍 |
| 服务之星 | `volunteer-stars.html` | 查看优秀志愿者展示 |
| 活动报名 | `volunteer-activity.html` | 浏览并报名志愿活动 |
| 我的消息 | `volunteer-message.html` | 查看通知消息 |
| 社区论坛 | `volunteer-forum.html` | 查看或参与社区交流 |
| 个人中心 | `volunteer-profile.html` | 查看和维护个人资料 |

### 8.2 志愿者注册

后端接口支持志愿者注册。

接口地址：

```text
POST /api/v1/volunteer/auth/register
```

请求示例：

```json
{
  "username": "student001",
  "password": "123456",
  "realName": "张三",
  "age": 20,
  "skills": "摄影、秩序维护",
  "preferences": "校园服务、环保活动"
}
```

注册结果：

| 情况 | 结果 |
|---|---|
| 用户名未被占用 | 注册成功，等待管理员审核 |
| 用户名已存在 | 注册失败，提示用户名已存在 |

注册后账号状态为 `PENDING`，需要管理员审核通过后才能登录。

### 8.3 志愿者登录

接口地址：

```text
POST /api/v1/volunteer/auth/login
```

请求示例：

```json
{
  "username": "volunteer",
  "password": "123456"
}
```

登录成功后返回：

```json
{
  "success": true,
  "message": "success",
  "data": {
    "token": "jwt-token",
    "userId": 3,
    "username": "volunteer",
    "role": "VOLUNTEER"
  }
}
```

登录限制：

- 密码必须正确
- 账号状态必须为 `ACTIVE`
- 待审核或禁用账号不能登录

### 8.4 浏览活动

接口地址：

```text
GET /api/v1/volunteer/activities
```

只返回状态为 `PUBLISHED` 的活动。

活动信息包括：

- 活动编号
- 活动标题
- 活动描述
- 活动要求
- 活动状态
- 负责人编号
- 最大招募人数
- 开始时间
- 结束时间

### 8.5 报名活动

接口地址：

```text
POST /api/v1/volunteer/applications
```

请求示例：

```json
{
  "activityId": 1
}
```

报名规则：

1. 只能报名已发布的活动。
2. 不能重复报名同一活动。
3. 活动审核通过人数不能超过最大招募人数。
4. 报名后默认状态为 `PENDING`，等待管理员审核。

报名状态说明：

| 状态 | 含义 |
|---|---|
| `PENDING` | 待审核 |
| `APPROVED` | 已通过 |
| `REJECTED` | 已拒绝 |

### 8.6 查看报名状态

接口地址：

```text
GET /api/v1/volunteer/applications/status
```

也可以通过“我的活动”功能查看已报名活动：

```text
GET /api/v1/volunteer/activities/my
```

### 8.7 活动签到

报名审核通过后，志愿者可以在活动开始时签到。

接口地址：

```text
POST /api/v1/volunteer/attendance/check-in
```

请求示例：

```json
{
  "activityId": 1
}
```

签到规则：

1. 必须已经报名该活动。
2. 报名状态必须为 `APPROVED`。
3. 同一活动不能重复签到。

### 8.8 活动签退

活动结束后，志愿者可以进行签退。

接口地址：

```text
POST /api/v1/volunteer/attendance/check-out
```

请求示例：

```json
{
  "activityId": 1
}
```

签退后系统会自动计算服务时长，并累计到志愿者个人总服务时长中。

### 8.9 个人中心

接口地址：

```text
GET /api/v1/volunteer/profile
```

个人资料包括：

- 用户编号
- 用户名
- 真实姓名
- 年龄
- 技能特长
- 服务偏好
- 累计服务时长
- 签到签退记录

### 8.10 修改个人资料

接口地址：

```text
PUT /api/v1/volunteer/profile
```

请求示例：

```json
{
  "realName": "张三",
  "age": 20,
  "skills": "摄影、秩序维护",
  "preferences": "校园服务、环保活动"
}
```

---

## 9. 管理员端操作说明

管理员端主要用于平台后台管理，包括志愿者审核、活动管理、报名审核和统计查看。

### 9.1 管理员首页

入口页面：

```text
frontend/admin.html
```

导航菜单包括：

| 菜单 | 页面 | 说明 |
|---|---|---|
| 首页 | `admin.html` | 查看平台数据概览 |
| 平台介绍 | `admin-about.html` | 查看平台介绍 |
| 志愿者管理 | `admin-volunteer.html` | 管理志愿者账号和资料 |
| 活动管理 | `admin-activity.html` | 创建、修改、删除活动 |
| 消息通知 | `admin-message.html` | 管理消息通知 |
| 数据统计 | `admin-statistics.html` | 查看统计数据 |
| 社区论坛 | `admin-forum.html` | 管理社区内容 |
| 个人中心 | `admin-profile.html` | 管理员个人信息 |

### 9.2 查看志愿者列表

接口地址：

```text
GET /api/v1/admin/volunteers
```

返回内容包括：

- 用户编号
- 用户名
- 账号状态
- 真实姓名
- 年龄
- 技能特长
- 服务偏好
- 累计服务时长

### 9.3 审核志愿者账号

接口地址：

```text
POST /api/v1/admin/volunteers/audit
```

请求示例：

```json
{
  "userId": 3,
  "status": "ACTIVE"
}
```

可设置状态：

| 状态 | 含义 |
|---|---|
| `PENDING` | 待审核 |
| `ACTIVE` | 启用 |
| `DISABLED` | 禁用 |

### 9.4 修改志愿者资料

接口地址：

```text
PUT /api/v1/admin/volunteers/{id}
```

可修改：

- 真实姓名
- 年龄
- 技能特长
- 服务偏好

### 9.5 创建活动

接口地址：

```text
POST /api/v1/admin/activities
```

请求示例：

```json
{
  "title": "校园文明劝导志愿服务",
  "description": "维护校园秩序，引导文明行为。",
  "requirements": "责任心强，按时到岗。",
  "organizerId": 2,
  "maxVolunteers": 30,
  "startTime": "2026-05-20T09:00:00",
  "endTime": "2026-05-20T12:00:00"
}
```

创建后的活动默认状态为 `DRAFT`。

### 9.6 修改活动

接口地址：

```text
PUT /api/v1/admin/activities/{id}
```

请求示例：

```json
{
  "status": "PUBLISHED",
  "maxVolunteers": 40
}
```

活动状态说明：

| 状态 | 含义 |
|---|---|
| `DRAFT` | 草稿 |
| `PUBLISHED` | 已发布，可报名 |
| `ONGOING` | 进行中 |
| `COMPLETED` | 已完成 |

### 9.7 删除活动

接口地址：

```text
DELETE /api/v1/admin/activities/{id}
```

删除活动时，与该活动相关的报名记录、签到记录会根据数据库外键级联关系删除。

### 9.8 查看活动报名记录

接口地址：

```text
GET /api/v1/admin/activities/{id}/applications
```

返回内容包括：

- 报名编号
- 活动编号
- 活动名称
- 志愿者编号
- 报名状态
- 报名时间

### 9.9 审核活动报名

接口地址：

```text
POST /api/v1/admin/activities/applications/audit
```

请求示例：

```json
{
  "applicationId": 1,
  "status": "APPROVED"
}
```

审核规则：

1. 审核状态不能回退为 `PENDING`。
2. 审核通过前会检查活动名额是否已满。
3. 审核通过后，志愿者才能签到。

### 9.10 查看后台统计数据

接口地址：

```text
GET /api/v1/admin/dashboard/stats
```

统计内容包括：

- 志愿者总数
- 活动总数
- 累计服务时长
- 报名总数
- 审核通过数量
- 报名通过率

---

## 10. 活动负责人端操作说明

活动负责人端主要用于管理本人负责的活动，包括查看活动、辅助签到签退、评价志愿者、提交活动总结。

### 10.1 活动负责人首页

入口页面：

```text
frontend/active.html
```

导航菜单包括：

| 菜单 | 页面 | 说明 |
|---|---|---|
| 首页 | `active.html` | 查看个人负责活动概览 |
| 平台介绍 | `active-about.html` | 查看平台说明 |
| 活动管理 | `active-activity.html` | 查看本人负责活动 |
| 现场管理 | `active-site.html` | 管理现场签到签退 |
| 活动总结 | `active-summary.html` | 提交活动总结 |
| 社交论坛 | `active-forum.html` | 参与交流 |
| 个人中心 | `active-profile.html` | 查看个人信息 |

### 10.2 查看本人负责的活动

接口地址：

```text
GET /api/v1/organizer/my-activities
```

系统根据当前登录活动负责人的 JWT 身份，查询其负责的活动列表。

### 10.3 辅助志愿者签到或签退

接口地址：

```text
POST /api/v1/organizer/attendance/assist-check
```

请求示例：

```json
{
  "activityId": 1,
  "volunteerId": 3,
  "checkIn": true
}
```

参数说明：

| 字段 | 说明 |
|---|---|
| `activityId` | 活动编号 |
| `volunteerId` | 志愿者编号 |
| `checkIn` | `true` 表示签到，`false` 表示签退 |

规则：

1. 活动负责人只能管理自己负责的活动。
2. 被签到志愿者必须报名通过。
3. 签退时系统会自动计算服务时长。

### 10.4 评价志愿者

接口地址：

```text
PUT /api/v1/organizer/attendance/{id}/evaluate
```

请求示例：

```json
{
  "evaluation": "服务态度认真，现场配合度高。"
}
```

规则：

- 只能评价本人负责活动下的签到记录。

### 10.5 提交活动总结

接口地址：

```text
POST /api/v1/organizer/activities/{id}/summary
```

请求示例：

```json
{
  "content": "本次活动整体顺利完成，志愿者准时到岗。",
  "feedback": "建议下次提前一天进行人员确认。"
}
```

规则：

1. 只能总结本人负责的活动。
2. 每个活动只能提交一次总结。
3. 提交总结后，活动状态自动变更为 `COMPLETED`。

---

## 11. 推荐业务流程

### 11.1 志愿活动完整流程

```text
管理员创建活动
  ↓
管理员发布活动
  ↓
志愿者浏览活动
  ↓
志愿者提交报名
  ↓
管理员审核报名
  ↓
志愿者签到
  ↓
志愿者签退
  ↓
活动负责人评价志愿者
  ↓
活动负责人提交活动总结
  ↓
活动完成
```

### 11.2 志愿者账号完整流程

```text
志愿者注册
  ↓
管理员审核账号
  ↓
志愿者登录
  ↓
浏览活动并报名
  ↓
参加活动并签到签退
  ↓
查看服务时长和个人记录
```

### 11.3 管理员日常操作流程

```text
管理员登录
  ↓
查看待审核志愿者
  ↓
审核志愿者账号
  ↓
创建并发布活动
  ↓
查看活动报名
  ↓
审核报名申请
  ↓
查看数据统计
```

### 11.4 活动负责人日常操作流程

```text
活动负责人登录
  ↓
查看本人负责活动
  ↓
现场协助签到/签退
  ↓
活动结束后评价志愿者
  ↓
提交活动总结
```

---

## 12. 接口调用示例

### 12.1 使用 curl 登录

```bash
curl -X POST http://localhost:8080/api/v1/volunteer/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"volunteer\",\"password\":\"123456\"}"
```

### 12.2 携带 Token 查询活动

```bash
curl -X GET http://localhost:8080/api/v1/volunteer/activities ^
  -H "Authorization: Bearer jwt-token"
```

### 12.3 报名活动

```bash
curl -X POST http://localhost:8080/api/v1/volunteer/applications ^
  -H "Content-Type: application/json" ^
  -H "Authorization: Bearer jwt-token" ^
  -d "{\"activityId\":1}"
```

---

## 13. 常见问题

### 13.1 注册后为什么不能登录？

志愿者注册后默认为 `PENDING` 待审核状态，需要管理员审核为 `ACTIVE` 后才能登录。

### 13.2 为什么活动报名失败？

可能原因：

1. 活动不是已发布状态。
2. 已经报名过该活动。
3. 活动名额已满。
4. 当前账号不是志愿者角色。

### 13.3 为什么不能签到？

可能原因：

1. 没有报名该活动。
2. 报名还未审核通过。
3. 已经签到过，不能重复签到。

### 13.4 为什么不能签退？

可能原因：

1. 还没有签到。
2. 已经签退过。

### 13.5 为什么活动负责人不能管理某个活动？

活动负责人只能管理自己作为 `organizer_id` 负责的活动。

### 13.6 后端启动失败怎么办？

可按以下顺序检查：

1. JDK 是否为 17。
2. Maven 是否正确安装。
3. MySQL 是否启动。
4. 数据库 `volunteer_system` 是否已经创建。
5. `application.yml` 中数据库账号密码是否正确。
6. 8080 端口是否被占用。

### 13.7 数据库连接失败怎么办？

检查：

1. MySQL 服务是否运行。
2. 数据库地址和端口是否正确。
3. 用户名和密码是否正确。
4. 是否已经执行 `schema.sql`。
5. MySQL 是否允许当前用户访问该数据库。

### 13.8 接口返回无权访问怎么办？

常见原因：

1. 未登录，没有携带 Token。
2. Token 已过期或格式错误。
3. 当前用户角色没有权限访问该接口。
4. 请求头没有按 `Authorization: Bearer <token>` 格式填写。

---

## 14. 维护说明

### 14.1 修改端口

在 `application.yml` 中修改：

```yaml
server:
  port: 8080
```

### 14.2 修改数据库连接

在 `application.yml` 中修改：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/volunteer_system
    username: root
    password: root
```

### 14.3 修改 JWT 过期时间

在 `application.yml` 中修改：

```yaml
app:
  jwt:
    expiration-minutes: 120
```

### 14.4 添加新接口

推荐步骤：

1. 在 `dto` 中新增请求和响应 DTO。
2. 在 `repository` 中补充数据查询方法。
3. 在 `service` 中实现业务逻辑。
4. 在 `controller` 中暴露 REST 接口。
5. 在 `SecurityConfig` 中确认权限规则。
6. 编译并测试接口。

---

## 15. 文档版本

| 版本 | 日期 | 说明 |
|---|---|---|
| V1.0 | 2026-05-19 | 补全系统操作、部署、接口、维护说明 |
